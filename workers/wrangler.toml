name = "clawtrl-webhook-proxy"
main = "webhook-proxy.ts"
compatibility_date = "2024-11-06"

# Set per-environment via `wrangler vars` or commit your repo here.
[vars]
GITHUB_REPO = "portalfnd/clawtrlautonomous"

# Secrets (set via `wrangler secret put`):
#   GITHUB_TOKEN         GitHub PAT with repo + workflow scopes
#   WEBHOOK_HMAC_SECRET  shared secret used to verify inbound webhook signatures
